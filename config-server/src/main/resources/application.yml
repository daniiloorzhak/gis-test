spring:
  application:
    name: 'config-server'
  cloud:
    config:
      server:
        git:
          uri: 'https://github.com/daniiloorzhak/gis-config'
          clone-on-start: true
server:
  port: 8888
